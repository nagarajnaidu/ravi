<h1>Editing product</h1>

<%= render 'form' %>

<%= link_to 'Show', @product %> |
<%= link_to 'Back', products_path %>


<%= form_for @product, :url => productss_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :photo %>
<% end %>

